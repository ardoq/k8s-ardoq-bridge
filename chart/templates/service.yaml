apiVersion: v1
kind: Service
metadata:
  name: {{ include "ardoq-k8s-bridge.fullname" . }}
  labels:
    {{- include "ardoq-k8s-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ardoq-k8s-bridge.selectorLabels" . | nindent 4 }}
