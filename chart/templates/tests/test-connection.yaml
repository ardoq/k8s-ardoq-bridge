apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ardoq-k8s-bridge.fullname" . }}-test-connection"
  labels:
{{ include "ardoq-k8s-bridge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ardoq-k8s-bridge.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
