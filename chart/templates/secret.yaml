apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ardoq-k8s-bridge.fullname" . }}
  labels:
  {{- include "ardoq-k8s-bridge.labels" . | nindent 4 }}
data:
  {{- if .Values.ardoq.apiKey }}
  ARDOQ_APIKEY: {{ .Values.ardoq.apiKey | b64enc | quote }}
  {{- end }}
